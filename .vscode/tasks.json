{
  // *
  "version": "2.0.0",
  "type": "shell",

  // *
  "tasks": [
    // ! Public

    // * Public-1
    {
      //
      "label": "Global: Install NPM",
      "detail": "Installs NPM globally.",
      "type": "shell",

      //
      "command": "bash",
      "args": ["scripts/global-install-npm.sh"]
    },

    // ! 1

    // * 1-1
    {
      //
      "label": "Install",
      "detail": "Runs npm install.",
      "type": "shell",

      //
      "command": "npm",
      "args": ["install"],

      //
      "dependsOn": ["Global: Install NPM"],
      "dependsOrder": "sequence"
    },

    // * 1-2
    {
      // *
      "runOptions": {
        "runOn": "folderOpen"
      },

      //
      "label": "Build",
      "detail": "Runs npm run build.",
      "type": "shell",

      //
      "command": "npm",
      "args": ["run", "build"],

      //
      "dependsOn": ["Install"],
      "dependsOrder": "sequence"
    },

    // ! 2

    // * 2-1
    {
      //
      "label": "Global: Install NPM Check Updates",
      "detail": "Installs npm-check-updates globally.",
      "type": "shell",

      //
      "command": "bash",
      "args": ["scripts/global-install-npm-check-updates.sh"],

      //
      "dependsOn": ["Global: Install NPM"],
      "dependsOrder": "sequence"
    },

    // * 2-2
    {
      // *
      "runOptions": {
        "runOn": "folderOpen"
      },

      //
      "label": "NPM Check Updates",
      "detail": "Runs npm-check-updates.",
      "type": "shell",

      //
      "command": "npm-check-updates",

      //
      "dependsOn": ["Global: Install NPM Check Updates"],
      "dependsOrder": "sequence"
    }
  ]
}
