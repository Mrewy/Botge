{
  "extends": "../tsconfig.base",
  "references": [],
  "compilerOptions": {
    // Language and Environment
    "emitDecoratorMetadata": true,
    "experimentalDecorators": true,

    // Emit
    "outDir": "../dist"
  }
}
