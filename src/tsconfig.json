{
  "extends": "../tsconfig.base",
  "references": [],
  "compilerOptions": {
    // Emit
    "outDir": "../dist",

    // Language and Environment
    "emitDecoratorMetadata": true,
    "experimentalDecorators": true
  }
}
